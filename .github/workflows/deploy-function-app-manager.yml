name: Deploy FunctionAppManager

on:
  push:
    branches:
      - main

env:
  AZURE_FUNCTIONAPP_NAME: 'Firepuma-PaymentsService-Manager'
  AZURE_FUNCTIONAPP_PACKAGE_PATH: 'Firepuma.Payments.FunctionAppManager'
  DOTNET_VERSION: '6.0'

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v2

      - name: Setup DotNet ${{ env.DOTNET_VERSION }} Environment
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install dependencies
        run: |
          dotnet restore Firepuma.Payments.Abstractions
          dotnet restore Firepuma.Payments.Client
          dotnet restore Firepuma.Payments.FunctionApp
          dotnet restore Firepuma.Payments.FunctionAppManager
          dotnet restore Firepuma.Payments.Tests

      - name: Build FunctionApp
        run: dotnet build --configuration Release --no-restore --output ./output
        working-directory: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}

      - name: Test
        run: dotnet test --no-restore

      - name: 'Deploy FunctionApp'
        uses: Azure/functions-action@v1
        id: deploy_functionapp
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: '${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}/output'
          publish-profile: ${{ secrets.AZURE_MANAGER_FUNCTIONAPP_PUBLISH_PROFILE }}